upstream tekon_web {
    server 127.0.0.1:8002;   # Gunicorn expuesto por docker-compose
}

server {
    listen 80;
    server_name tekon-rl.cl;

    location /favicon.ico { 
        access_log off; 
        log_not_found off; 
        }

    location /static/ {
        alias /ruta/al/proyecto/shared/staticfiles/;
    }

    location /media/ {
        alias /ruta/al/proyecto/shared/media/;
    }

    location / {
        proxy_pass http://tekon_web;
        proxy_set_header Host                $host;
        proxy_set_header X-Real-IP           $remote_addr;
        proxy_set_header X-Forwarded-For     $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto   $scheme;
        proxy_set_header X-Forwarded-Host    $server_name;
    }

    add_header X-Content-Type-Options nosniff;
    add_header X-Frame-Options DENY;
    add_header X-XSS-Protection "1; mode=block";

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    gzip_min_length 256;
}

server {
    listen 80;
    server_name redlinegs.com;

    location /favicon.ico { 
        access_log off; 
        log_not_found off; 
        }

    location /static/ {
        alias /ruta/al/proyecto/shared/staticfiles/;
    }

    location /media/ {
        alias /ruta/al/proyecto/shared/media/;
    }

    location / {
        proxy_pass http://tekon_web;
        proxy_set_header Host                $host;
        proxy_set_header X-Real-IP           $remote_addr;
        proxy_set_header X-Forwarded-For     $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto   $scheme;
        proxy_set_header X-Forwarded-Host    $server_name;
    }

    add_header X-Content-Type-Options nosniff;
    add_header X-Frame-Options DENY;
    add_header X-XSS-Protection "1; mode=block";

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    gzip_min_length 256;
}

server {
    listen 80;
    server_name www.tekon-rl.cl;
    return 301 http://tekon-rl.cl$request_uri;
}

server {
    listen 80;
    server_name www.redlinegs.com;
    return 301 http://redlinegs.com$request_uri;
}